#include "directX.h"





int main(int argc, char ** argv)
{
    int fbfd = startX(argv[0]);
char c[]="im love marina.";
   
   
    if (fbfd == -1) exit(1);
    if ((int)fbp == -1)exit(1);


for(int xy=0;xy<vinfo.xres-20;xy=xy+20)rectangle(xy,xy,xy+20,xy+20,0,0,255);


refresh(); 
endX(fbfd);
    return 0;
}










